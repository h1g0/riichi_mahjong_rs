[target.wasm32-unknown-unknown]
# Hand-written JavaScript plugins provide these imports when Macroquad
# instantiates the module; wasm-bindgen remains intentionally unused.
rustflags = [
    '--cfg',
    'getrandom_backend="custom"',
    '-C',
    'link-arg=--allow-undefined',
]
